<?xml version="1.0" encoding="utf-8"?>
<!--
  Production network security config.
  All backend traffic goes to Render (HTTPS) — cleartext is explicitly blocked.
  System trust anchors (Let's Encrypt) cover *.onrender.com certificates.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs (covers Let's Encrypt / Render SSL) -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for localhost/emulator during local development,
         and for the Hetzner production backend (raw IP over HTTP, no domain yet).
         TODO: remove 167.233.94.60 once PROD moves to https://<domain>. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="false">167.233.94.60</domain>
    </domain-config>
</network-security-config>
